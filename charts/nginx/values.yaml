replicaCount: 1

image:
  repository: nginx
  tag: stable
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - /
  tls: []

# Example optional subchart toggle (exercise)
redis:
  enabled: false

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
