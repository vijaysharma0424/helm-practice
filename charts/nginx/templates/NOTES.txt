1. To access the application after install:

  - Port-forward: kubectl port-forward svc/{{ include "nginx.fullname" . }} 8080:{{ .Values.service.port }}
  - Then open http://127.0.0.1:8080

2. To remove the release: `helm uninstall <release-name>`
